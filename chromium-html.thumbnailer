# This one doesn't work, as firefox really wants a profile
# and we would have to set up a profile in a separate step
[Thumbnailer Entry]
TryExec=/usr/bin/chromium
Exec=/usr/bin/chromium --headless --temp-profile --screenshot %o %i --window-size 256x256 %s
MimeType=text/html
